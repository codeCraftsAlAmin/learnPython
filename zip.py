# # Zip: with the help of zip we can combine multiple list in a sinlge list.

roll=[101, 102, 103, 104]
name=["al amin", "newz", "abir", "rahat"]
section=["A", "B", "C", "D"]

print(list(zip(roll, name)))
print(list(zip(roll, name, section)))